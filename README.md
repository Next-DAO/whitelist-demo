# Contract demos for whitelist verification

## Getting Started

1. Install dependencies:

```zsh
yarn install
```

2. Start the hardhat node on localhost

```zsh
yarn chain
```

3. Open a new terminal window and run unit tests for demo

```zsh
yarn test
```
